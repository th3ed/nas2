#!/usr/bin/env bash
set -euo pipefail
exec docker run --rm --gpus all \
    -e DISPLAY="${DISPLAY:-}" \
    -v /tmp/.X11-unix:/tmp/.X11-unix \
    -v "${HOME}/.openclaw:/root/.openclaw" \
    {{ openclaw_image }} "$@"

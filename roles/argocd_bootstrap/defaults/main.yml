---
argocd_namespace: argocd
argocd_chart_version: "7.7.10"
argocd_chart_repo: "https://argoproj.github.io/argo-helm"

# Override in group_vars/all/main.yml
gitops_repo_url: "https://github.com/th3ed/nas2.git"
gitops_repo_branch: "main"
gitops_apps_path: "gitops/apps"

bitwarden_namespaces:
  - ollama
  - openclaw
  - monitoring
  - litellm
  - hermes
  - agentregistry
  - mem0
  # news-rag is retained until PR 2 removes the old Application; the migration
  # Job in the new `news` chart reads from the old DB during cutover.
  - news-rag
  - news

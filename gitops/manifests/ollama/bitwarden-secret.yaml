---
# Placeholder — Ollama itself takes no secrets today, but adding this file
# documents the namespace's bw-auth-token wiring and gives a slot to add
# secrets later (e.g. an OPENAI_API_KEY proxy override) without re-bootstrapping.
#
# The bw-auth-token Secret is created in this namespace by the argocd_bootstrap
# Ansible role. Uncomment + fill in when a secret is needed.
#
# apiVersion: k8s.bitwarden.com/v1
# kind: BitwardenSecret
# metadata:
#   name: ollama-secrets
#   namespace: ollama
# spec:
#   organizationId: REPLACE_WITH_BITWARDEN_ORG_ID
#   secretName: ollama-secrets
#   authToken:
#     secretName: bw-auth-token
#     secretKey: token
#   map: []
